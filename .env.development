# Default development environment variables for StackBlitz/blink.new
# These provide sensible defaults so the app can run without additional configuration

# Application Settings
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_SITE_URL=http://localhost:3000
NODE_ENV=development

# Supabase (Mock values - replace with real values for full functionality)
NEXT_PUBLIC_SUPABASE_URL=https://placeholder.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=placeholder-anon-key
SUPABASE_SERVICE_ROLE_KEY=placeholder-service-role-key

# Stripe (Mock values - replace with real test keys for payment functionality)
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_placeholder
STRIPE_SECRET_KEY=sk_test_placeholder
STRIPE_WEBHOOK_SECRET=whsec_placeholder

# Note: For full functionality, you'll need to:
# 1. Create a Supabase project at https://supabase.com
# 2. Get Stripe test keys at https://dashboard.stripe.com/test/apikeys
# 3. Replace the placeholder values above with your actual keys
